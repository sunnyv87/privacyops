apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  NODE_ENV: "production"
  API_PORT: "4000"
  WEB_PORT: "3000"
  OPENSEARCH_URL: "http://{{ .Values.opensearch.host }}:{{ .Values.opensearch.port }}"
  NATS_URL: {{ .Values.nats.url | quote }}
  TEMPORAL_ADDRESS: {{ .Values.temporal.address | quote }}
  TEMPORAL_NAMESPACE: "privacyops"
  S3_REGION: {{ .Values.s3.region | quote }}
  S3_BUCKET: {{ .Values.s3.bucket | quote }}
  CORS_ORIGINS: "https://app.privacyops.techd.com"
  LOG_LEVEL: "log"
  OTEL_ENABLED: {{ .Values.observability.tracing.enabled | quote }}
  OTEL_SERVICE_NAME: {{ .Values.observability.tracing.serviceName | quote }}
  OTEL_EXPORTER_OTLP_ENDPOINT: {{ .Values.observability.tracing.endpoint | quote }}
  WORKER_HEALTH_PORT: "4001"
