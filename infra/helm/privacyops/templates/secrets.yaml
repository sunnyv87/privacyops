apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
type: Opaque
data:
  # These values must be base64-encoded in production
  # Use: echo -n "value" | base64
  jwt-secret: {{ "change-me-in-production" | b64enc | quote }}
